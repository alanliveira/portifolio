import { Container } from "@/components/layout/Container";
import { SectionHeading } from "@/components/ui/SectionHeading";
const flow = ["Problema", "Usuário", "Solução", "Arquitetura", "Código", "Produção", "Feedback", "Evolução"];
export function ProductMindset() { return <section className="section"><Container><SectionHeading eyebrow="Produto" title="Tecnologia com visão de produto." description="Não começo escolhendo uma tecnologia. Começo entendendo o problema, quem utiliza a solução, quais são as regras do contexto e como o produto precisará evoluir." sticker="aws" /><ol className="mt-12 flex flex-col gap-3 lg:flex-row lg:items-center lg:gap-0">{flow.map((item, index) => <li key={item} className="flex items-center gap-3 lg:flex-1"><span className="flex min-h-12 flex-1 items-center justify-center rounded-lg border border-white/10 bg-white/3 px-3 text-center text-xs font-bold uppercase tracking-wider text-slate-200">{item}</span>{index < flow.length - 1 ? <span className="text-accent" aria-hidden="true">→</span> : null}</li>)}</ol></Container></section>; }
